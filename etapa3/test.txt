int function() {
    int a <= 1, b, c <= 2;
    b = 3;
    if (2 + 3) then {
        while (ident) {
            a = - 1;
        };
    } else {
        b = !7;
    };
    fun(a, 2, c);
    return 2;
}
